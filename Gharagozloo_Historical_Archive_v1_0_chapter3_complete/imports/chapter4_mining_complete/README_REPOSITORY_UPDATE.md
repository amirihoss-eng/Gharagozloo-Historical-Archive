# Repository update instructions

This ZIP is the completed **Chapter 4 mining evidence package**.

It contains structured records ready to merge into the canonical archive. It does **not** replace the existing SQLite master because the Chapter 3-complete SQLite file is not present in this chat's working storage.

To produce the final repository-ready release with an updated `archive.sqlite` and numbered SQL migration, provide the current repository ZIP or the current master SQLite file. The records in this package can then be mapped to the existing stable identifiers without risking duplicate IDs or schema mismatch.

Recommended target release name:

`Gharagozloo_Historical_Archive_v1_1_chapter4_complete`
